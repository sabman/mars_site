-title "MARS Users"
- User.all.each do |user| 
  .section
    %p
      %strong=link_to user.full_name, user
    %p=user.department
    %p=user.office_phone
    %p=link_to user.email, "mailto:#{user.email}"
